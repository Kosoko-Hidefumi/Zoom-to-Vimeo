# 引き継ぎマニュアル：Zoom録画のダウンロード〜Vimeoアップロード〜オミットリスト作成（手動版・ノーコード）

このマニュアルは、`main.py` / `vimeo_upload.py` などの**スクリプトを一切使わず**、
Zoomの管理画面とVimeoの管理画面、Excelだけを使って、これまで自動化されていた作業を
人手で再現するための手順書です。

対象読者：プログラミング未経験の担当者

---

## 0. 全体の流れ（サマリー）

```
① consultant_vimeo.csv（進行表）を確認・準備
        ↓
② Zoom管理画面でクラウド録画を検索してダウンロード
        ↓
③ ダウンロードしたファイルを「講師名フォルダ／日付フォルダ」に整理し、
   ファイル名を「配信用動画タイトル」にリネーム
        ↓
④ 進捗管理表（ダウンロード結果）に記録
        ↓
⑤ Vimeoにアップロードし、タイトル・パスワードを設定
        ↓
⑥ アップロード結果表（vimeo_results 相当）に記録（リンクを貼る）
        ↓
⑦ 必要な列だけを抜き出した一覧表（vimeo_links 相当）を作る
        ↓
⑧ リンクが取得できた行だけを残した「オミットリスト」（vimeo_links_omit 相当）を作る
```

---

## 1. 事前準備・必要なもの

| 項目 | 内容 |
|---|---|
| Zoomアカウント | クラウド録画の閲覧・ダウンロード権限（管理者 or 録画のホスト） |
| Vimeoアカウント | 動画のアップロード・プライバシー設定ができる権限 |
| 進行表 | `consultant_vimeo.csv`（Excelで開ける。UTF-8） |
| 保存先PC | ある程度の空き容量（録画は1本あたり数百MB〜数GB） |
| Excel | 進捗管理・結果記録用 |

### `consultant_vimeo.csv` の列（重要な列のみ）

| 列名 | 用途 |
|---|---|
| 講師名（英語） | フォルダ名・一覧表に使用 |
| 日付 | Zoom録画を探すときの絞り込み条件 |
| 開始時刻 | 同日に複数会議がある場合の絞り込み条件 |
| 配信用動画タイトル | **ダウンロードファイル名・Vimeoの動画タイトルに使う最重要項目** |
| ミーティングID | Zoomでの検索キー |
| パスコード | **Vimeoの視聴パスワードに設定する値** |
| 専門科・所属 | 一覧表作成時に転記 |

> ⚠️ 「配信用動画タイトル」が空欄の行は、この後の突合ができないため、
> 事前に埋めておいてください（空欄のままだとスキップ対象になります）。

---

## 2. Zoomクラウド録画のダウンロード（手動）

### 2.1 Zoom管理画面にログイン

1. https://zoom.us にブラウザでログイン
2. 左メニュー「レコーディング」→「クラウドレコーディング」を開く

### 2.2 進行表の行ごとに録画を探す（突合のルール）

進行表を1行ずつ確認し、以下の条件で対象の録画を特定します。これは自動ツールが
内部で行っていた突合ロジックと同じです。

1. **ミーティングID**でクラウドレコーディング一覧を検索（画面上部の検索欄にミーティングIDを入力。半角スペースは無視してOK）
2. 該当ミーティングが複数の日付にまたがって出てくる場合は、**進行表の「日付」と一致するもの**を選ぶ（見つからない場合は前日・翌日もあわせて確認）
3. 同じ日に開始時刻が近い録画が複数ある場合は、**進行表の「開始時刻」との差が±90分以内**のものを選ぶ
4. 1つの録画に複数のファイル種別（レコーディングタイプ）がある場合は、次の優先順位で1つだけ選ぶ：
   1. `shared_screen_with_speaker_view`（画面共有＋発表者ビュー）★最優先
   2. `shared_screen_with_speaker_view(CC)`
   3. `shared_screen_with_gallery_view`
   4. `active_speaker`
   5. `gallery_view`
   6. `shared_screen`
   - **必ずMP4形式のファイルのみ**を対象にする（M4A音声のみ、チャット、字幕ファイルなどは対象外）
   - 同じ優先順位の中に複数ファイルがある場合は**ファイルサイズが最大のもの**を選ぶ
5. 録画のステータスが「処理中」の場合はまだダウンロードできません。後日再確認してください。
6. 上記条件に合うMP4が見当たらない場合は、その行は「未取得」として進捗管理表に記録し、次の行へ進みます。

### 2.3 ダウンロード実行

1. 対象の録画の行で「ダウンロード」ボタンをクリック（MP4のみ）
2. ダウンロード完了までブラウザを閉じない
3. パスコード付きの録画の場合、閲覧パスコードの入力を求められることがあります（進行表の「パスコード」列とは別物の場合があるので、Zoom側の案内に従ってください）

---

## 3. ダウンロードファイルの整理（フォルダ・リネームのルール）

自動ツールは以下のルールでファイルを保存していました。手作業でも同じ構成に揃えてください。
（Vimeoアップロード時の突合や、後工程の検索性を保つために重要です）

### 3.1 フォルダ構成

```
downloads/
 └─ 講師名（英語）フォルダ/         例）Dr. Michele Pangilinan
      └─ 日付フォルダ（YYYY-MM-DD）/   例）2025-07-14
           └─ 配信用動画タイトル.mp4
```

- 講師名フォルダ名：進行表の「講師名（英語）」をそのまま使用（英語名が無ければ日本語名）
- 日付フォルダ名：`YYYY-MM-DD` 形式（例：`2025-07-14`）

### 3.2 ファイル名（重要）

ダウンロードしたZoomのファイル名を、**進行表の「配信用動画タイトル」と完全に一致する名前**にリネームしてください（拡張子は `.mp4`）。

例：
- 配信用動画タイトル：`[産婦人科] Dr. Michele Pangilinan - Neurobiology of Addiction (2025.07.14)`
- 保存ファイル名：`[産婦人科] Dr. Michele Pangilinan - Neurobiology of Addiction (2025.07.14).mp4`

**ファイル名に使えない文字の置き換えルール**（Windows/Mac共通で問題になりやすい記号）：

| 使えない文字 | 置き換え |
|---|---|
| `< > : " / \ \| ? *` | `_`（アンダースコア）に置換 |
| 先頭・末尾の半角スペースやピリオド | 削除 |
| アンダースコアの連続（`__`など） | 1つにまとめる |
| 200文字を超える場合 | 200文字で切り詰める |

> 💡 このファイル名（拡張子を除いた部分）が、後の「Vimeoアップロード時の突合キー」になります。
> **進行表の「配信用動画タイトル」とファイル名（拡張子なし）が完全一致していること**を必ず確認してください（全角/半角スペース・大文字小文字の違いは吸収されますが、それ以外の表記ゆれはNGです）。

---

## 4. 進捗管理表の作成（Zoom結果CSVの手動版）

Excelで以下の列を持つ表を作成し、進行表の行ごとに記録します（ファイル名は例：`result_進捗_YYYYMMDD.xlsx`）。

| 列名 | 内容 |
|---|---|
| 講師名（英語） | 進行表から転記 |
| 日付 | 進行表から転記 |
| 配信用動画タイトル | 進行表から転記 |
| ステータス | 下表参照 |
| 備考 | 補足（見つからない理由など） |

**ステータスの選択肢（自動ツールと同じ表記に揃える）**

| ステータス | 意味 |
|---|---|
| `downloaded` | ダウンロード成功・確定ファイル名にリネーム済み |
| `no_zoom_url` | 進行表にZoom URL/ミーティングIDが無い（対象外） |
| `not_found` | 該当ミーティングID・日付の録画が見つからない |
| `not_ready` | 録画が処理中でまだダウンロードできない |
| `download_error` | ダウンロード中にエラーが発生 |
| `rename_error` | ダウンロードはできたがリネームに失敗 |

---

## 5. Vimeoへのアップロード（手動）

### 5.1 事前準備：重複チェック

アップロード作業を始める前に、**Vimeoに既に同じタイトルの動画がアップロードされていないか**を確認してください（二重アップロード防止）。

1. Vimeoにログイン → 「マイビデオ」一覧を開く
2. 進行表の「配信用動画タイトル」で検索し、同名（表記ゆれを除きほぼ同一）の動画が無いか確認
3. 既にある場合は、その行を進捗表で `skipped(already_on_vimeo)` として記録し、アップロードをスキップ

### 5.2 アップロード手順

1. Vimeoにログイン
2. 「新規アップロード」→ 該当のMP4ファイルを選択してアップロード
3. **タイトル**：進行表の「配信用動画タイトル」を**そのまま**入力（128文字を超える場合は先頭128文字まで）
4. **プライバシー設定**：
   - 「表示設定」→「パスワードで保護された人のみ」を選択
   - パスワード欄に、進行表の「パスコード」列の値を入力
5. アップロード・設定変更を保存
6. アップロード完了後、動画の共有リンク（`https://vimeo.com/xxxxxxxxx` の形式）をコピー

### 5.3 進行表とは別に「Vimeo作業表」を作る

Zoomのダウンロード結果とVimeoのアップロード結果は**別の表で管理**します（自動ツールでも `result_*.csv` と `vimeo_results.csv` を分けています）。

Excelで以下の列を持つ表を作ります（ファイル名例：`vimeo_results_手動_YYYYMMDD.xlsx`）。
進行表（`consultant_vimeo.csv`）の全列をコピーし、右側に3列追加するのが簡単です。

| 追加列名 | 内容 |
|---|---|
| status | 下表参照 |
| video_uri | （任意）VimeoのビデオID。例：`/videos/1154202805` |
| link | アップロード後にコピーした共有リンク。例：`https://vimeo.com/1154202805` |

**status の選択肢**

| status | 意味 |
|---|---|
| `uploaded` | 今回アップロード成功 |
| `skipped(already_on_vimeo)` | Vimeoに既に同名動画があったためスキップ |
| `skipped(already_uploaded)` | 前回の作業表で既に `uploaded` 済みだったためスキップ |
| `missing` | 進行表にはあるが、対象のMP4ファイルが手元に無い（③のダウンロードが未完了） |
| `error:◯◯` | アップロード中にエラー発生（内容をメモ） |

> ⚠️ 「配信用動画タイトル」が空欄の行はアップロード対象から除外してください。

---

## 6. Vimeoリンク一覧表の作成（`vimeo_links.xlsx` 相当）

「5.3 Vimeo作業表」から、**必要な列だけを抜き出した一覧表**を新しいExcelシート（またはブック）として作成します。人に配布・共有する用の整形済みリストです。

**抜き出す列（この順番・この列名で）**

1. 講師名（英語）
2. 専門科
3. 所属
4. レクチャータイトル（英語）
5. パスコード
6. link

**作り方（Excel手動）**

1. 新しいシートを作成し、上記6列の見出しをA1〜F1に入力
2. 「5.3 Vimeo作業表」から該当列の値をコピー＆貼り付け（値のみ貼り付け推奨）
3. 見た目を整える（任意）：
   - 見出し行：背景色（濃い青系）＋白文字＋太字
   - 1行おきに薄い色を付けると見やすい
   - link列：セルにハイパーリンクを設定（`Ctrl+K` などで実際のURLをリンク化）
   - 「所属」「レクチャータイトル（英語）」列は折り返して表示
   - 見出し行を固定表示（ウィンドウ枠の固定）
4. ファイル名を `vimeo_links.xlsx` として保存（全行を含む版）

---

## 7. オミットリストの作成（`vimeo_links_omit.xlsx` 相当）★最終成果物

「6. Vimeoリンク一覧表」（`vimeo_links.xlsx`）をコピーして、**link列が空欄（＝Vimeoアップロードが完了していない・リンクが取得できていない）行を削除**したものを作成します。

**作り方**

1. `vimeo_links.xlsx` を複製し、ファイル名を `vimeo_links_omit.xlsx` に変更
2. link列（F列）でフィルターをかけ、**空白セル、または「http」で始まっていない値**の行を特定
3. 該当する行をすべて削除（非表示ではなく削除）
4. 残った行が「Vimeoへの公開が完了している動画のみのリスト」になっていることを確認
5. 保存

> 💡 判定基準：`link` の値が `http` から始まっている行だけを残す（自動ツールの判定条件と同一）。
> それ以外（空欄、`missing`、`skipped` で未アップロードなど）は除外します。

**チェックポイント**

- [ ] `vimeo_links_omit.xlsx` に含まれる行はすべて実際にVimeoで再生確認できるか
- [ ] リンク先のパスワードが進行表の「パスコード」と一致しているか
- [ ] タイトルが進行表の「配信用動画タイトル」と一致しているか

---

## 8. （任意）完了後の公開ギャラリー更新について

本マニュアルはオミットリスト作成までを手動化する範囲です。その先の
「`vimeo_metadata_export.csv` を使ったギャラリー自動デプロイ（GitHub Actions → Vercel）」は
コードによる自動化の仕組みのため、この手動フローの対象外です。
ギャラリーサイトへの反映が必要な場合は、開発担当者に `vimeo_links_omit.xlsx` の内容を共有してください。

---

## 9. 作業チェックリスト（1講義あたり）

- [ ] 進行表で「配信用動画タイトル」「パスコード」「ミーティングID」が埋まっている
- [ ] Zoom管理画面で該当録画を特定（ミーティングID＋日付＋開始時刻±90分、MP4のみ、優先タイプ）
- [ ] MP4をダウンロード
- [ ] 「講師名フォルダ／日付フォルダ」に整理し、ファイル名を配信用動画タイトルにリネーム
- [ ] 進捗管理表にステータスを記録
- [ ] Vimeoで同名動画の重複が無いか確認
- [ ] Vimeoにアップロード（タイトル・パスワード設定）
- [ ] Vimeo作業表にstatus・linkを記録
- [ ] `vimeo_links.xlsx` に転記
- [ ] link空欄の行を除いて `vimeo_links_omit.xlsx` を作成

---

## 10. トラブルシューティング（手動版）

| 現象 | 対処 |
|---|---|
| Zoomで該当ミーティングIDの録画が見つからない | 日付を前後1日ずらして確認。ミーティングIDの空白・桁数の入力ミスを確認 |
| 同じ日に候補が複数ある | 開始時刻が進行表と一番近いものを選ぶ（±90分以内） |
| 録画が複数タイプある | 本マニュアル 2.2-4 の優先順位表に従う（画面共有＋発表者ビュー優先） |
| ダウンロードしたファイル名がタイトルと違う | 手動でリネーム（3.2の禁止文字ルールを確認） |
| Vimeoアップロード後に再生できない/パスワードが違う | プライバシー設定を再確認（「パスワードで保護」＋進行表の「パスコード」列） |
| 同じ動画を二重アップロードしてしまった | 古い方（または重複している方）をVimeo側で削除し、作業表を修正 |
| `vimeo_links_omit.xlsx` に未公開の行が混ざっている | link列が空欄/http以外の行を再度フィルターして削除 |

以上です。
